{
  "name": "project-redwood",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "test": "node --test test/*.test.mjs",
    "m1:validate": "node src/method/validate.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
