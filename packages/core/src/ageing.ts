import type { Claim, DealScope, MethodProfile, Receivable } from "./types.js";
const dayMs = 86_400_000;
export function ageBucket(days: number): string { if (days <= 30) return "0-30"; if (days <= 60) return "31-60"; if (days <= 90) return "61-90"; if (days <= 120) return "91-120"; return "121+"; }
export function deriveReceivableAge(receivable: Receivable, claim: Claim | undefined, scope: DealScope, method: MethodProfile): Pick<Receivable, "ageDays" | "ageBucket"> | undefined { const basis = method.ageingBasis; const date = basis === "service_date" ? claim?.serviceDate : basis === "claim_submission_date" ? claim?.submissionDate : receivable.lineage.raw.billing_date ?? receivable.lineage.raw.Billing_Date; if (!date || !/^\d{4}-\d{2}-\d{2}$/.test(date)) return undefined; const days = Math.floor((Date.parse(`${scope.valuationDate}T00:00:00Z`) - Date.parse(`${date}T00:00:00Z`)) / dayMs); if (days < 0) return undefined; return { ageDays: days, ageBucket: ageBucket(days) }; }
