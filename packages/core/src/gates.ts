import type { CanonicalDeal, Finding, FindingDisposition, Gate, MappingDecision, MethodProfile, Reconciliation, ReviewDecision, SourceType } from "./types.js";
export interface GateStatus { gate: Gate; status: "open" | "passed" | "blocked"; rationale: string; }
function activeDecision(decision: ReviewDecision, fingerprint?: string): boolean { return !decision.invalidatedAt && (fingerprint === undefined || decision.dependencyFingerprint === fingerprint); }
function activeDisposition(disposition: FindingDisposition, fingerprint?: string): boolean { return !disposition.invalidatedAt && (fingerprint === undefined || disposition.dependencyFingerprint === fingerprint); }
export function evaluateGates(input: { deal: CanonicalDeal; mappings: MappingDecision[]; reconciliations: Reconciliation[]; findings: Finding[]; decisions: ReviewDecision[]; dispositions?: FindingDisposition[]; methodProfile?: MethodProfile; dependencyFingerprint?: string; calculationPassed?: boolean }): GateStatus[] {
  const approved = (gate: Gate) => input.decisions.some((decision) => decision.gate === gate && decision.decision === "approved" && activeDecision(decision, input.dependencyFingerprint)); const required = new Set<SourceType>(["claims", "payments", "ar_snapshot", "general_ledger"]); const present = new Set(input.mappings.map((mapping) => mapping.sourceType)); const mappingsReady = input.mappings.length > 0 && input.mappings.every((mapping) => mapping.canonicalField && mapping.reviewStatus === "approved"); const policy = input.methodProfile?.reconciliationDispositionPolicy; const dispositions = input.dispositions ?? []; const accepted = (finding: Finding) => dispositions.some((d) => d.findingId === finding.id && d.decision === "accepted_with_exception" && d.evidenceReferences.length > 0 && activeDisposition(d, input.dependencyFingerprint)); const reconFindings = input.findings.filter((finding) => finding.kind === "reconciliation"); const reconClear = input.reconciliations.every((reconciliation) => reconciliation.arithmeticStatus === "matched") || (policy?.acceptedWithExceptionMayPassG3 === true && reconFindings.length > 0 && reconFindings.every(accepted)); const findingsClear = input.findings.every((finding) => finding.status === "resolved" || finding.status === "reviewed" || accepted(finding));
  const gates: GateStatus[] = [{ gate: "G0", status: approved("G0") ? "passed" : "open", rationale: approved("G0") ? "Scope decision approved." : "Scope approval has not been recorded." }, { gate: "G1", status: [...required].every((type) => present.has(type)) && input.deal.quarantined.length === 0 ? "passed" : "blocked", rationale: "Required source populations must be present and invalid rows quarantined/reviewed." }, { gate: "G2", status: mappingsReady ? "passed" : "blocked", rationale: "Every proposed mapping must have a canonical target and reviewer approval." }, { gate: "G3", status: reconClear ? "passed" : "blocked", rationale: reconClear ? "Reconciliations passed or active policy-permitted exceptions are evidenced." : "Unreconciled differences cannot be suppressed by an approval." }, { gate: "G4", status: input.calculationPassed === false ? "blocked" : "passed", rationale: input.calculationPassed === false ? "Calculation engine did not complete successfully." : "Deterministic engine completed for this run." }, { gate: "G5", status: approved("G5") ? "passed" : "open", rationale: "Professional assumptions/judgments require an explicit approval." }, { gate: "G6", status: findingsClear && approved("G6") ? "passed" : "blocked", rationale: "Findings must have active dispositions and review before release." }, { gate: "G7", status: "blocked", rationale: "Evaluating prerequisite gates." }]; const prerequisiteGates = gates.filter((item) => item.gate !== "G7"); const g7 = gates.find((item) => item.gate === "G7")!; const failed = prerequisiteGates.filter((item) => item.status !== "passed").map((item) => item.gate); if (failed.length === 0 && approved("G7")) { g7.status = "passed"; g7.rationale = "All prerequisite gates passed and an active run-bound G7 release decision exists."; } else g7.rationale = failed.length ? `Release blocked by prerequisite gates: ${failed.join(", ")}.` : "Release approval has not been recorded."; return gates;
}
